# ملاحظات نشر Telegram Media Suite على Render

## الحالة الحالية

تم التحقق من تسجيل الدخول إلى Render عبر حساب المستخدم، وظهر الحساب ومساحة العمل والمشروع الحالي. تم إنشاء مستودع GitHub خاص باسم `srhnaahmd-cyber/telegram-media-suite` ورفع الإصدار `3.1.0` إلى الفرع `main`، ثم أُضيفت تحسينات الإصدار الإنتاجي الحالية إلى نسخة العمل المنفصلة.

أُضيفت ملفات `render.yaml` و`scripts/render_start.py`. تستخدم الخدمة أمر البناء `pip install -r requirements.txt` وأمر التشغيل `python3 scripts/render_start.py`. يجهز سكربت البدء مجلد البيانات والجلسة، ثم يشغل Gunicorn من مجلد `app` بعامل واحد لتجنب تكرار polling الخاص بالبوت. أُضيفت نقطة الصحة العامة `/health`، ويستخدم التطبيق منفذ `PORT` وواجهة `0.0.0.0` على Render، ولا يفتح المتصفح في بيئة الإنتاج.

## حماية الإنتاج

لوحة الإدارة وجميع مسارات التحكم وواجهات التنزيل محمية بمفتاح `TMD_DASHBOARD_TOKEN`. يدعم التطبيق إرسال المفتاح في `X-Dashboard-Token` أو كـ `Bearer`، كما يسمح بتسجيل دخول Basic Auth باستخدام المفتاح ككلمة مرور للوصول التفاعلي إلى الواجهة. نقطة `/health` فقط عامة للمراقبة، ولا تعرض الأسرار أو السجلات أو قائمة المستخدمين.

يُعطّل التسجيل الذاتي للمستخدمين افتراضيًا في Render عبر `SELF_ENROLLMENT_ENABLED=false`. لذلك لا يستطيع مستخدم غير موجود في قائمة السماح إضافة نفسه بمجرد الضغط على «نعم»؛ يجب أن يضيف المالك User ID يدويًا من إعدادات الخدمة السرية أو الواجهة المحمية.

| المتغير | الغرض | طريقة الإدخال |
|---|---|---|
| `API_ID` و`API_HASH` | بيانات تطبيق Telegram | Render Secret Environment Variables |
| `BOT_TOKEN` | تشغيل بوت Telegram | Render Secret Environment Variables |
| `PHONE` | الحساب الشخصي المستخدم للجلب | Render Secret Environment Variables |
| `ALLOWED_USER_IDS` | قائمة المستخدمين المسموح لهم | Render Environment Variable محمية |
| `TMD_SESSION_B64` | نسخة Base64 من جلسة الحساب | Render Secret Environment Variable |
| `TMD_DASHBOARD_TOKEN` | حماية الواجهة وواجهات التحكم | Render Secret Environment Variable |
| `SELF_ENROLLMENT_ENABLED` | التحكم في التسجيل الذاتي | القيمة الإنتاجية `false` |
| `TMD_SUITE_DATA` | مسار البيانات المؤقتة | `/opt/render/project/src/.tmd-data` |
| `TMD_HOST` و`TMD_OPEN_BROWSER` | إعداد الاستضافة | `0.0.0.0` و`false` |

## الاختبارات المنفذة

نجحت اختبارات الوظائف السابقة، واختبار استيراد Gunicorn، واختبار حماية مسارات Flask، واختبار منع التسجيل الذاتي، واختبار حساب نسبة التنزيل والسرعة والوقت المتبقي. أصبح callback التنزيل يحدّث حالة مشتركة فقط، بينما ينفذ بوت Telegram نبضًا غير متزامن كل ثانيتين على حلقة البوت؛ وهذا يمنع مشكلة اختلاف الحلقات ويعرض النسبة والسرعة ووقت الانتظار بصورة مستقرة.

## تنبيه التخزين في الخطة المجانية

الخطة المجانية لا توفر قرصًا دائمًا؛ لذلك يعاد تجهيز الجلسة من `TMD_SESSION_B64` عند كل تشغيل. الملفات التي ينشئها التطبيق ليست مخزنًا دائمًا للإنتاج، ويجب تنزيل النتائج المهمة أو الانتقال إلى خطة بقرص دائم إذا أصبح الاحتفاظ طويل المدى مطلوبًا.

## إصلاح فحص القناة

عند اختيار «فحص القناة كاملًا» أو استخدام النطاق القديم `١` ثم `٠`، يطلب البوت الآن سنة البداية ثم الشهر ثم اليوم عبر مفاتيح Telegram. يبدأ الجلب من بداية اليوم المختار حتى الوقت الحالي، ثم يزيل الرسائل ذات المعرف المكرر ويرتب الوسائط من الأقدم إلى الأحدث. يعرض التقرير لكل عنصر رقم الرسالة والتاريخ والكاتب وUser ID وUsername عند توفرها ونوع الوسيط واسم الملف الأصلي والوصف.

## خطوات الإطلاق المتبقية

بعد التأكد من وجود جميع المتغيرات في النموذج، تُنشأ الخدمة من زر **Deploy web service**. تراقب سجلات البناء حتى نجاح Gunicorn، ثم يُختبر `/health` خارجيًا، ويُختبر البوت من أحد User IDs المسموح بها. بعد نجاح ذلك فقط يُنشأ HTTP monitor خارجي على `/health` بفاصل خمس دقائق، ثم يُوقف الخادم المحلي المؤقت والجسر الحساس وتُحذف ملفاتهما المؤقتة.


## آخر حالة النشر

الخدمة العامة هي `https://telegram-media-suite.onrender.com`، ومعرّف خدمة Render هو `srv-da8arrcs728c73b6sd00`. ظهر الإصدار `69b8899` بحالة Live، ثم بدأ Render نشر commit `cb30613` تلقائيًا بعد إضافة تشخيص أخطاء بدء جلسة Telegram؛ كانت العملية ما تزال في حالة Building عند آخر متابعة. يجب إعادة فحص حالة هذا النشر قبل اعتماد الإصدار النهائي.


## حواجز منع التكرار

أضيف حارس لتحديث Telegram يعتمد على معرف المحادثة ومعرف الرسالة، مع مهلة احتفاظ محدودة، وحارس زمني لردود `/start` حتى لا ينتج عن وصول التحديث نفسه أو الضغط المتكرر سيل من رسائل الترحيب. كما أصبحت روابط التنزيل المتطابقة بعد إزالة الاستعلامات مدمجة في مهمة واحدة، وصار الاختيار الجماعي يمنع تشغيل طابور ثانٍ أو إرسال العنصر نفسه مرتين.

## تحديث دعم الروابط
أضيف في التحديث التالي محلل موحد للروابط يلتقط الصيغ العامة والخاصة وروابط الموضوعات، والاختصار `/s/`، و`telegram.me` و`telegram.dog`، وروابط الدعوة `+hash` و`joinchat`، وروابط Telegram العميقة `tg://resolve` و`privatepost` و`msg_url` و`openmessage`. كما أصبحت الروابط المتعددة تُستخرج من النص الحر وتُزال تكراراتها وفق القناة ورقم الرسالة قبل إدخالها إلى خط الأتمتة.
