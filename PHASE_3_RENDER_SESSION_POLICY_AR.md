# سياسة Render والجلسة في النسخة المستقلة

## القرار

تظل ملفات Render ولوحة التحكم وإدارة الجلسة والتشغيل والإلغاء الموجودة في خط أساس `7dde66c` كما هي. لم يتم نسخ `render_start.py` الحديث من `main`، ولم يتم استبدال ملفات `app/config_store.py` أو `app/web_app.py` أو `scripts/render_start.py` أو `render.yaml` داخل الفرع المستقل.

## سبب الإبقاء

النسخة المستقلة يجب أن تستخدم قلب `7dde66c` دون خلط معماري مع النسخة الحديثة. نقل ملف تشغيل Render الحديث كاملًا قد يربط لوحة التحكم بواجهات أو أسماء وحدات لا تنتمي إلى بنية `7dde66c`، وقد يؤدي إلى استبدال منطق التنزيل أو تشغيل أكثر من عميل Telegram للحساب نفسه.

## الملفات المحمية في هذه المرحلة

| المجال | الملفات |
|---|---|
| تشغيل Render | `scripts/render_start.py` و`render.yaml` |
| لوحة التحكم | `app/web_app.py` |
| الجلسة والإعدادات | `app/config_store.py` |
| قلب النظام | `app/automation.py` و`app/bot_service.py` و`app/downloader.py` و`main.py` |

## ضمانات التشغيل

يبقى مسار الجلسة ومسار التخزين وآلية التشغيل والإلغاء كما وردت في خط الأساس. لا تُحذف جلسة موجودة ولا تُستبدل تلقائيًا، ولا تُضاف عملية تشغيل ثانية للحساب نفسه. أي تغيير في هذه الملفات يحتاج قرارًا منفصلًا واختبارات خاصة قبل اعتماده.

## نتيجة المرحلة

تمت مراجعة الفروقات بين `7dde66c` و`main`، وثبت أن إبقاء ملفات Render والجلسة من `7dde66c` هو الخيار الأكثر أمانًا لنسخة الاختبار الحالية، مع إبقاء `main` والفروع الأصلية دون تعديل.
إعداد: Manus AI
