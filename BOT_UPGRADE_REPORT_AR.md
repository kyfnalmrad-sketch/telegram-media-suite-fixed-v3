# تقرير تطوير Telegram Media Suite

## الإصدار ٣.١.٠ — تحديث الأتمتة

حُفظت نسخة الإصدار السابق ٣.٠.٠ في مجلد مستقل قبل بدء التحديث. يضيف الإصدار ٣.١.٠ قاعدة النطاق المفتوح `١ إلى ٠`، ومعالجين متوازيين لتنزيل عدة روابط ووسائط، وتقريرًا لا يوقف العملية كلها عند فشل عنصر واحد، وتسجيلًا ذاتيًا للحسابات الجديدة بعد موافقة صريحة.

## الإصلاح المنفذ

كان خطأ `FileNotFoundError` يحدث لأن Telegram قد يعيد مسارًا مختلفًا عن المسار المتوقع بعد اكتمال التنزيل. عالجت النسخة الجديدة ذلك عبر التحقق من المسار الذي يعيده المحرك، ثم تجربة المسار المتوقع، ثم البحث عن أحدث ملف يحمل رقم الرسالة داخل مجلد القناة. إذا لم يوجد الملف بعد ذلك يظهر خطأ واضح بدل محاولة إرسال مسار غير موجود.

## القائمة التفاعلية

عند إرسال `/start` تظهر صورة الذئب الترحيبية المضمّنة مع لوحة أزرار دائمة أسفل مربع الكتابة، وتشمل ابدأ، عودة، فحص رابط، فحص قناة، تنزيل رابط، نطاقًا بالرسائل، نطاقًا بالوقت، الحالة، والقائمة. بعد اختيار أي عملية يطلب البوت المدخل التالي تلقائيًا. وتبقى أزرار Inline مخصصة لنتائج الفحص، مثل تنزيل ملف واحد أو كل ملفات النطاق، مع أزرار ابدأ وعودة.

عند اختيار **فحص قناة** يمكن إرسال رابط قناة عامة، أو رابط رسالة داخل قناة، أو رابط دعوة. يحلل البوت الرابط ويعرض اسم القناة ونوعها ومعرّفها وما إذا كان لها اسم مستخدم عام، ثم يسأل المستخدم هل هو مشترك. إذا كانت القناة العامة قابلة للقراءة، يمكن اختيار «لا» وسيجلب النظام البيانات دون إجبار المستخدم على الاشتراك. إذا احتاج الوصول إلى عضوية، يعرض زر «نعم انضم»؛ وبعد موافقة صريحة يحاول الحساب الشخصي الانضمام إلى القناة العامة أو رابط الدعوة، ثم يعيد التحقق ويتابع. لا يتم أي انضمام دون موافقة، ولا يمكن تجاوز طلبات الانضمام أو صلاحيات القنوات الخاصة. بعد نجاح الوصول يتيح فحص القناة كاملًا، أو تحديد بداية ونهاية الرسائل، أو اختيار فترة زمنية بتوقيت UTC. ولطلب كل الرسائل استخدم البداية `١` والنهاية `٠`؛ ويظهر تقرير بعدد الرسائل المفحوصة وعدد الوسائط.

الأوامر المدعومة هي:

```text
/info https://t.me/channel/123
/download https://t.me/channel/123
/range https://t.me/channel 1 50
/status
/menu
```

يمكن إرسال عدة روابط في رسالة واحدة، كل رابط في سطر أو مفصولًا بمسافة. يزيل البوت الروابط المكررة ويقبل حتى ٢٠ رابطًا في العملية الواحدة. في الفحص المتعدد يعرض تفاصيل مستقلة لكل رابط، ثم يوفر تنزيل كل الملفات الناجحة أو تنزيل ملف محدد. وفي التنزيل المتعدد يعمل معالجان فقط بطابور محدود، ويعرض شريط تقدم لكل ملف وعدد الناجح والفاشل. فشل عنصر لا يوقف بقية العناصر.

النطاق الرقمي المحدد محصور بحد أقصى ١٠٠ رسالة في الطلب الواحد، أما الصيغة الخاصة `١ إلى ٠` فتعني قراءة كل الرسائل المتاحة ثم إنشاء تقرير شامل. تُنظم الملفات بهذا الشكل:

```text
مسار_التخزين/اسم_القناة/YYYY_MM/اسم_الملف_الأصلي.ext
```

يحافظ النظام على اسم الملف الأصلي وامتداده كما أرسلهما Telegram. لا يدخل وصف الرسالة أو رقمها في الاسم؛ وتُستخدم لاحقة تصادم تقنية فقط إذا تكرر الاسم داخل المجلد نفسه.

## التشخيص عند التعذر

لم يعد البوت يكتفي بعبارة عامة أو اسم استثناء. عند تعذر الفحص أو الوصول أو التنزيل يعرض **السبب** و**الإصلاح المقترح**. فالرابط غير الصحيح يعرض الصيغة المقبولة، والقناة الخاصة غير المتاحة توضح ضرورة أن يكون الحساب عضوًا فيها، والرسالة المحذوفة تطلب نسخ الرابط من جديد، ومشكلة الجلسة تطلب تسجيل الدخول، ومشكلة مسار التخزين توضح ضرورة اختيار مجلد قابل للكتابة. لا تُعرض الأسرار أو محتويات ملفات الجلسة في الرسائل.

## الاسم والوصف السياقي

اسم الملف الخارجي يبقى مطابقًا للاسم الأصلي قدر الإمكان. أما الوصف فيُعرض منفصلًا في نتيجة الفحص وتعليق الملف المرسل. إذا لم توجد تسمية في الرسالة، يبحث النظام في الرسائل الخمس التالية؛ وإذا وجد ملفات متتابعة يأخذ أسماءها الأصلية أو أنواعها، ثم يضيف أول رسالة نصية/وصفية لاحقة إلى السياق. بذلك يمكن أن يظهر مثلًا: `part-one — part-two — وصف الحلقة` دون تغيير اسم الملف الأصلي.

## التحسينات المنفذة في هذا الإصدار

| التحسين | الحالة |
|---|---|
| لوحة أزرار دائمة أسفل مربع الكتابة عند `/start` | منفذ |
| فحص رابط قناة أو رسالة داخل قناة | منفذ |
| اختيار فحص كامل أو نطاق رسائل أو فترة زمنية | منفذ |
| عرض سبب التعذر والإصلاح المقترح | منفذ |
| طلب الرابط تلقائيًا بعد اختيار العملية | منفذ |
| طلب رقم البداية والنهاية خطوة بخطوة | منفذ |
| عرض معلومات القناة والرسالة والحجم والنوع | منفذ |
| اختيار ملف واحد أو كل ملفات النطاق | منفذ |
| إصلاح المسار قبل إرسال الملف | منفذ |
| حماية البوت بقائمة User IDs | منفذ |
| حفظ الجلسة واستخدامها لاحقًا | منفذ |
| تنظيم حسب القناة والشهر مع الحفاظ على الاسم الأصلي | منفذ |
| وصف سياقي منفصل من الرسائل والملفات التالية | منفذ |
| دعم عدة روابط مع إزالة التكرار وتقرير مستقل | منفذ |
| جلب بيانات القنوات العامة دون اشتراط الاشتراك عند السماح | منفذ |
| سؤال الاشتراك ومحاولة الانضمام بعد موافقة صريحة | منفذ |
| شريط تقدم حي للتنزيل بجانب الملف | منفذ |
| صلاحية أزرار النتائج ٢٤ ساعة | منفذ |
| أزرار ابدأ وعودة في لوحة ونتائج البوت | منفذ |
| تسجيل User ID الجديد عند /start بعد موافقة المستخدم | منفذ |
| صورة ذئب ترحيبية للبوت وخلفية الواجهة | منفذ |
| قاعدة النطاق المفتوح ١ إلى ٠ مع تقرير شامل | منفذ |
| معالجان متوازيان بطابور محدود | منفذ |
| شريط تقدم مستقل لكل ملف | منفذ |
| إخفاء الأسرار من السجل والحزمة | منفذ |

## خطة الأتمتة ذات ٢٥ ميزة

التفاصيل الكاملة محفوظة في `AUTOMATION_25_FEATURES_AR.md`. تشمل الخطة معالجة عدة قنوات ووسائط، الطابور الثنائي، عزل الأخطاء، التقارير، شريط التقدم، الفحص المفتوح، الفلاتر، الاستئناف، الجدولة، كشف التكرار، والأرشفة.

## عشرة تحسينات قوية مقترحة للمرحلة التالية

| الرقم | التحسين | الفائدة |
|---:|---|---|
| ١ | تنزيل دفعي مع قائمة انتظار | معالجة عدة نطاقات وروابط دون تدخل مستمر |
| ٢ | استئناف التنزيل بعد الانقطاع | متابعة الملف من آخر بايت بدل البدء من جديد |
| ٣ | شريط تقدم داخل Telegram | عرض النسبة والسرعة والوقت المتبقي برسالة محدثة |
| ٤ | كشف الملفات المكررة | منع تكرار التنزيل باستخدام رقم الرسالة أو بصمة الملف |
| ٥ | مرشحات نوع وحجم وتاريخ | تنزيل الفيديو أو الصور أو المستندات وفق شروط محددة |
| ٦ | إعدادات تسمية مخصصة | اختيار قالب مثل القناة والتاريخ ورقم الرسالة واسم الملف |
| ٧ | أرشفة ZIP اختيارية | تجميع نطاق كامل في ملف واحد قبل إرساله |
| ٨ | جدولة ومهام دورية | فحص القنوات تلقائيًا في أوقات محددة |
| ٩ | لوحة إحصاءات وتقارير | عدد الملفات والحجم والنجاح والفشل لكل قناة |
| ١٠ | إدارة صلاحيات متعددة | إضافة مستخدمين وصلاحيات منفصلة للمشاهدة والتنزيل والإدارة |

## حالة الاختبار الحالية

نجح فحص الصياغة والاختبار الذاتي واختبار الاسم الأصلي واختبار سلسلة الوصف المتتابع واختبار استخراج عدة روابط وإزالة التكرار واختبار صيغ روابط الدعوة والموضوع واختبار شريط التقدم وتدفق الوصول العام واختبار المعالج الثنائي واختبار النطاق المفتوح واختبار التسجيل الذاتي بالموافقة.
 كما تم تشغيل واجهة الويب محليًا والبوت المؤقت مع الجلسة المحفوظة. لم تُضمّن أي أسرار داخل الحزمة.

## التسجيل الذاتي للحسابات الجديدة

عند إرسال `/start` من حساب غير موجود في القائمة، لا يُرفض الطلب مباشرة. يعرض البوت User ID الذي استلمه Telegram ويطلب «نعم» أو «لا». عند الموافقة يحفظ User ID في قائمة السماح داخل `settings.json`، ثم يرسل لوحة الأزرار. تنتهي نافذة الموافقة بعد عشر دقائق. هذا يجعل البوت مناسبًا لعدة حسابات، مع بقاء القرار صريحًا من الحساب نفسه. وللحفاظ على خصوصية صارمة، يمكن تعطيل هذا الأسلوب والاعتماد على القائمة اليدوية فقط.

## طريقة التجربة

افتح البوت `@Trisns_bot` وأرسل `/start`. جرّب لوحة الأزرار، ثم اختر **فحص قناة** وأرسل رابط القناة أو رابط رسالة داخلها. اختر بعد التحليل الفحص الكامل أو نطاق الرسائل أو الفترة الزمنية. ولتجربة تشخيص الخطأ أرسل رابطًا غير صالح؛ سيعرض البوت السبب والإصلاح بدل رسالة عامة.

الخدمة المؤقتة تعمل للتجربة. بعد الانتهاء يجب إيقافها وتدوير Bot Token مرة أخرى إذا رغبت في إنهاء الاختبار.
