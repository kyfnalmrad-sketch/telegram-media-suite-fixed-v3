# تعليمات تشغيل Telegram Media Suite على Windows

## محتويات الحزمة

المجلد `app` يحتوي على واجهة الويب وخدمة البوت ومحرك جلسة Telegram. المجلد `assets` يحتوي على صورة الذئب المستخدمة في الواجهة ورسالة الترحيب. المجلد `scripts` يحتوي على ملفات التشغيل والاختبارات والبناء. ملف `requirements.txt` يحتوي على المتطلبات. لا تحتوي الحزمة على Bot Token أو `api_hash` أو رقم الهاتف أو ملفات الجلسات.

## التشغيل من المصدر

ثبّت Python 3.11 أو أحدث على Windows، ثم فك ضغط الحزمة في مجلد لا يحتاج إلى صلاحيات مسؤول. افتح PowerShell داخل مجلد المشروع ونفّذ:

```powershell
py -3 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe app\web_app.py
```

يمكن بدلًا من ذلك النقر مرتين على `scripts\run_windows.bat`. ستفتح الواجهة محليًا على `http://127.0.0.1:8765`.

## الإعداد الأول

أدخل `api_id` و`api_hash` ورقم الهاتف وBot Token من الواجهة. اختر مسارًا قابلًا للكتابة للتخزين، وأدخل User ID المسموح له باستخدام البوت، ثم اضغط حفظ الإعدادات. اضغط بدء جلسة Telegram وأكمل رمز التحقق وكلمة مرور التحقق بخطوتين عند طلبها. بعد ظهور حالة الجلسة `ready` اضغط تشغيل البوت.

تُحفظ القيم الحساسة في ملف `.env` داخل مجلد بيانات التطبيق، وتحفظ الإعدادات غير الحساسة في `settings.json`. لا تنسخ ملف `.env` أو مجلد `sessions` إلى أي شخص، ولا تضعهما داخل الحزمة التي سترسلها.

## تجربة البوت

افتح `@Trisns_bot` وأرسل `/start`. ستظهر صورة الذئب الترحيبية ولوحة أزرار دائمة أسفل مربع الكتابة. اختر **فحص قناة**، ثم أرسل رابط قناة عامة أو رابط رسالة داخل قناة، مثل:

```text
https://t.me/channel
https://t.me/c/3328229190/168/378?single
```

سيعرض البوت القناة ونوع الرابط والمعرّف، ثم يسأل: هل أنت مشترك في القناة؟ إذا كانت القناة العامة قابلة للقراءة، يمكنه جلب البيانات حتى مع إجابة «لا» دون إجبارك على الاشتراك. وإذا احتاج الوصول إلى عضوية، سيعرض خيار **«نعم انضم»**؛ عند موافقتك يحاول الحساب الشخصي الانضمام إلى القناة العامة أو رابط الدعوة، ثم يعيد التحقق ويتابع. روابط القنوات الخاصة التي تحتاج موافقة إدارية لا يمكن تجاوزها؛ عندها اشترك يدويًا ثم تابع. لا يتم أي انضمام دون موافقتك.

بعد نجاح الوصول اختر فحص القناة كاملًا، أو تحديد نطاق رسائل، أو تحديد فترة زمنية. استخدم زر **«ابدأ»** لإظهار العمليات من جديد، وزر **«عودة»** للرجوع للقائمة. تبقى أزرار اختيار الملفات صالحة لمدة ٢٤ ساعة، وبعدها استخدم «ابدأ» لإجراء فحص جديد. عند التنزيل يظهر **شريط تقدم حي** بجانب العملية، ويتحدث بالنسبة المئوية والحجم الحالي والحجم الكلي، ثم يرسل البوت الملف بعد اكتمال التنزيل.
 الفترة الزمنية تستخدم التوقيت العالمي UTC، وصيغتها التقنية هي `YYYY-MM-DD HH:MM`.

يمكن اختيار **فحص رابط** أو **تنزيل رابط** ثم إرسال عدة روابط في رسالة واحدة، كل رابط في سطر أو مفصولًا بمسافة. يقبل النظام صيغًا مرنة مثل `t.me/channel/123` و`https://telegram.me/channel/123` وروابط الموضوع الخاصة مثل `/c/3328229190/168/378?single`. الحد الأقصى ٢٠ رابطًا، وتُحذف المكررات. يعرض الفحص تفاصيل مستقلة لكل رابط، ويعرض التنزيل الجماعي ملخص النجاح والفشل.

## الاسم والوصف

يحافظ النظام على اسم الملف الأصلي وامتداده. لا يضيف الوصف أو رقم الرسالة إلى اسم الملف. إذا تكرر الاسم في المجلد نفسه تُستخدم لاحقة تصادم تقنية فقط. أما الوصف فيظهر منفصلًا في نتيجة الفحص وتعليق الملف. يبحث النظام في الرسائل التالية؛ فإذا وجد ملفات متتابعة يأخذ أسماءها أو أنواعها، ثم يلتقط الرسالة النصية الوصفية اللاحقة عند توفرها.

## عند ظهور تعذر

لا يكتفي البوت برسالة عامة. سيعرض سبب التعذر والإصلاح المقترح. للرابط غير الصحيح أرسل صيغة Telegram كاملة. إذا تعذر الجلب دون اشتراك سيقترح الانضمام بعد موافقتك، وإذا كانت القناة الخاصة تحتاج موافقة إدارية فاشترك يدويًا. للرسالة المحذوفة انسخ الرابط من Telegram من جديد. لمشكلة الجلسة أعد تسجيل الدخول من الواجهة. لمشكلة التخزين اختر مجلدًا قابلًا للكتابة وتأكد من وجود مساحة كافية.

## بناء EXE

على Windows، افتح PowerShell داخل المشروع ونفّذ:

```powershell
scripts\build_windows.bat
```

ينتج البناء مجلدًا قابلًا للتشغيل داخل `dist\TelegramMediaSuite\`. انقل المجلد كاملًا إلى جهاز التشغيل، ولا تضع الأسرار داخله. يجب تنفيذ تسجيل الدخول من الواجهة على جهاز التشغيل عند الحاجة.

## إيقاف التشغيل

أغلق نافذة PowerShell لإيقاف الواجهة والبوت المؤقت. إذا انتهت التجربة، أوقف البوت ودوّر Bot Token من BotFather قبل استخدامه في بيئة أخرى.
